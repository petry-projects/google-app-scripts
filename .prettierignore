# The dev-lead caller stub is a standards-owned artifact copied VERBATIM per
# petry-projects/.github/standards/ci-standards.md#5-dev-lead-agent. Its exact bytes
# (including the two-space gap before the `# NOSONAR` marker) are enforced by the
# central byte-identity stub-drift guard. Prettier would collapse that gap and
# re-introduce drift, so it must not reformat this file.
.github/workflows/dev-lead.yml
# initiative-driver is a standards-owned artifact copied verbatim from
# petry-projects/.github/standards/workflows/initiative-driver.yml.
# Prettier must not reformat it to avoid byte-drift on future syncs.
.github/workflows/initiative-driver.yml
# feature-ideation is a standards-owned caller stub whose centrally-owned
# `on:`/`permissions:`/`concurrency:` surface is enforced byte-identical against
# petry-projects/.github/standards/workflows/feature-ideation.yml by the stub-drift
# guard. The canonical uses single-quoted YAML scalars; Prettier would rewrite them
# to double quotes and re-introduce surface drift, so it must not reformat this file.
.github/workflows/feature-ideation.yml
